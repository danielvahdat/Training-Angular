// Generated by CoffeeScript 1.10.0
(function() {
  angular.module('myApp').controller('loginController', function($scope, $state, $location, authenticateService, sessionService) {
    $scope.login = function() {
      if ($scope.emailModel === 'admin' && $scope.passModel === 'password') {
        authenticateService.login($scope.emailModel);
        $state.go('home',{email:$scope.emailModel});
      } else if ($scope.emailModel === 'superadmin' && $scope.passModel === 'password') {
        authenticateService.login($scope.emailModel);
        $state.go('home',{email:$scope.emailModel});
      } else {
        alert('email or password is not correct');
      }
    };
  });

}).call(this);
